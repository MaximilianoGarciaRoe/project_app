<div class="card">
  <div class="card-header">
    <h1>Task Details</h1>
  </div>
  <div class="card-body">
    <h2 class="card-title"><%= @task.name %></h5>
    <h3 class="card-title"><%= @task.status %></h5>
    <h3 class="card-title"><%= link_to @task.project.name, project_path(@task.project) %></h5>
  </div>
    <div class="card-footer">
    <%= link_to 'Back', tasks_path, class: 'btn btn-secondary' %>
  </div>
</div>